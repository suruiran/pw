# pw

Human-friendly interfaces for any CLI.

`pw` (Prompt Wrapper) turns complex command-line tools into structured interactive interfaces through schemas.

# todos

- [ ]  A simple Python agent script for LLM interaction, with validation to ensure the generated JSON/TOML conforms to the expected format.
